import React from "react"

export const LoginForm = () => {
    // NOTE VARIABLES AND STATES

    // NOTE FUNCTIONS
  
    // NOTE RETURNS
    return (
        <div className="content-body-wrapper">
            <h2 className="bayon-regular">Login Form Here.</h2>  
        </div>
    )
}

export default LoginForm;